<div ng-controller="PullRequestIndexCtrl">
  <input type="text" ng-model="search" />
  <ul>
    <li ng-repeat="pullRequest in pullRequests | filter:search">
      {{ pullRequest.title }}
    </li>
  </ul>
</div>
